# VM2 — Image-Server
#   sudo ./install.sh --role image-server --non-interactive
# Optional: --image-server-host 10.0.0.11

GP_ROLE=image-server
GP_NON_INTERACTIVE=1
IMAGE_SERVER_ROOT=/srv/gamepanel-images
IMAGE_SERVER_HOST=10.0.0.11
IMAGE_SERVER_USER=gamepanel-images
IMAGE_SERVER_ENABLE_FTPS=no
